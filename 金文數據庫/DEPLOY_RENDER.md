# 正确部署流程说明

## ⚠️ 重要：GitHub 不能直接运行应用！

### 常见错误

**错误做法：**
```
访问：https://github.com/用户名/金文数据库
结果：404 错误
原因：GitHub 只是代码托管，不是运行平台
```

**正确做法：**
```
GitHub (存储代码) → Render (运行应用) → 用户访问
访问：https://金文数据库.onrender.com
```

---

## 📋 完整部署步骤

### 第1步：确认文件完整性

确保以下文件存在：

**必需文件：**
- ✅ `app.py` - Flask 应用
- ✅ `index_login.html` - 前端页面
- ✅ `script_login.js` - 前端脚本
- ✅ `styles.css` - 样式文件
- ✅ `requirements.txt` - Python 依赖
- ✅ `render.yaml` - Render 配置

**检查命令：**
```bash
dir app.py index_login.html script_login.js styles.css requirements.txt render.yaml
```

### 第2步：更新 GitHub 仓库

```bash
cd "d:\金文數據庫"
git add .
git commit -m "添加 render.yaml"
git push origin master
```

### 第3步：部署到 Render

#### 3.1 访问 Render

打开浏览器：https://render.com

#### 3.2 登录

点击 **Sign In**，选择 **Sign in with GitHub**

#### 3.3 创建 Web Service

1. 点击右上角 **"New +"**
2. 选择 **"Web Service"**
3. 点击 **"Connect GitHub"**

#### 3.4 连接仓库

1. 授权 Render 访问 GitHub
2. 在仓库列表找到 `金文数据库`
3. 点击 **Connect**

#### 3.5 配置部署

确认以下信息：

**基础配置：**
- **Name**: 金文数据库
- **Region**: Singapore 或 Oregon
- **Branch**: main 或 master（根据你的仓库）

**构建配置：**
- **Runtime**: Python 3
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python app.py`

**实例配置：**
- **Instance Type**: Free

#### 3.6 创建服务

点击 **"Create Web Service"** 按钮

#### 3.7 等待部署

- 部署时间：3-5 分钟
- 状态会从：Building → Deployed → Live

#### 3.8 获取 URL

部署成功后，在 Dashboard 显示：
```
https://金文数据库-xxxx.onrender.com
```

**这就是你要访问的 URL！**

---

## ✅ 验证部署成功

### 1. 检查状态

在 Render Dashboard，确保：
- ✅ Status 显示 **Live**
- ✅ 没有红色错误
- ✅ 可以点击 URL

### 2. 访问应用

**正确访问：**
```
✅ https://金文数据库-xxxx.onrender.com
```

**错误访问：**
```
❌ https://github.com/用户名/金文数据库
❌ https://github.com/用户名/金文数据库/blob/main/index_login.html
```

### 3. 测试功能

- ✅ 页面正常显示
- ✅ 可以输入密码登录
- ✅ 数据可以加载
- ✅ 搜索功能正常

---

## 🔧 故障排除

### 问题1：404 错误

**原因：**
- 访问了 GitHub URL 而不是 Render URL

**解决：**
使用 Render 提供的 `.onrender.com` URL

### 问题2：部署失败

**检查日志：**
在 Render Dashboard → 点击 Logs

**常见错误：**

**错误：Module not found**
```
解决：检查 requirements.txt 是否包含 Flask 和 Flask-CORS
```

**错误：File not found**
```
解决：确认所有文件都已上传到 GitHub
```

**错误：Permission denied**
```
解决：检查文件权限或路径
```

### 问题3：数据库连接失败

**原因：**
Render 的文件系统是临时的

**临时方案：**
部署后需要重新导入数据

**永久方案：**
改用 PostgreSQL（需要修改代码）

---

## 📚 参考文档

- Render 官方文档：https://render.com/docs
- Flask 部署指南：https://flask.palletsprojects.com/en/2.3.x/deploying/
- GitHub 使用指南：https://docs.github.com

---

## 💡 快速检查清单

部署前检查：
- [ ] 所有必需文件已上传到 GitHub
- [ ] render.yaml 配置正确
- [ ] requirements.txt 包含所有依赖
- [ ] app.py 路由配置正确
- [ ] GitHub 仓库公开（非私有）

部署后检查：
- [ ] Render Status 显示 Live
- [ ] 访问 Render URL（不是 GitHub）
- [ ] 页面正常显示
- [ ] 登录功能正常
- [ ] 数据加载成功

---

## 🎯 总结

**关键点：**
1. GitHub = 存放代码
2. Render = 运行代码
3. 访问 Render URL 才能使用应用

**正确流程：**
```
本地代码 → 推送到 GitHub → Render 部署 → 访问 Render URL
```

**不要这样做：**
```
❌ 访问 GitHub URL 想运行应用
❌ 在 GitHub 上查找 .onrender.com URL
```

**这样做：**
```
✅ 在 Render Dashboard 查看 URL
✅ 使用 Render 提供的 URL 访问应用
✅ 从 Render 获取访问地址
```

---

## 需要帮助？

如果仍然遇到问题：
1. 查看 `TROUBLESHOOTING.md` 详细故障排除
2. 检查 Render 日志
3. 确认所有文件都上传到 GitHub
4. 使用正确的 Render URL 访问应用
